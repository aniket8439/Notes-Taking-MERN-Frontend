export class NewNote {
    constructor(title, descr, cdate, importance) {

        this.title = title;
        this.descr = descr;
        this.cdate = cdate;
        this.importance = importance;
    }
}